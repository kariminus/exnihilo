{% extends 'ExNihiloPlatformBundle:Back:index.html.twig' %}

{% block content %}
    <h3>Modifier l'événement</h3>
    <div class="row text-center">
        {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <div class="cell-1-1 text-align-center"><button type="submit">Modifier</button></div>
        {{ form_end(edit_form) }}
    </div>
    <ul style="list-style: none">
        <li>
            <a href="{{ path('admin_event_index') }}">Retour à la liste des événements</a>
        </li>
    </ul>
{% endblock %}
