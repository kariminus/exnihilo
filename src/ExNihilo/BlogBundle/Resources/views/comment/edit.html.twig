{% extends 'ExNihiloPlatformBundle:Back:index.html.twig' %}

{% block content %}
    <h3>Modifier le commentaire</h3>

    <div class="row text-center">
        {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <p><div class="cell-1-1 text-align-center"><input type="submit" value="Modifier"/></div></p>
        {{ form_end(edit_form) }}
    </div>

    <ul style="list-style: none">
        <li>
            <a href="{{ path('admin_comment_index') }}">Revenir à la liste des commentaires</a>
        </li>
    </ul>
{% endblock %}
