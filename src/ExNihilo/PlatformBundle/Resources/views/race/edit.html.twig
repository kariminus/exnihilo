{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modifier une race</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('admin_race_index') }}">Revenir à la liste</a>
        </li>
    </ul>
{% endblock %}
